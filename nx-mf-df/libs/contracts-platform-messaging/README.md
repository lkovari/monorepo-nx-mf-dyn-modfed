# contracts-platform-messaging

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build contracts-platform-messaging` to build the library.

## Running unit tests

Run `nx test contracts-platform-messaging` to execute the unit tests via [Jest](https://jestjs.io).
